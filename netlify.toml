[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22"

# Static SPA only. Account + Rebbe APIs live on Supabase Edge Functions.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
